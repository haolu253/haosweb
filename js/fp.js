new fullpage('#fullpage', {
    autoScrolling: true,
    dragAndMove: true,
    fitToSection: true,
    navigation: true,
    navigationPosition: 'left',
    anchors: ['s1', 's2', 's3', 's4'],
    responsiveSlides: true,
    // afterRender: function () {
    //     $('video').get(0).play();
    // }
})