new fullpage('#fullpage', {
    autoScrolling: true,
    dragAndMove: true,
    fitToSection: true,
    navigation: true,
    navigationPosition: 'left',
    anchors: ['s1', 's2', 's3', 's4', 's5']
})

// $('.section-content').addClass('d-none')vaguely

// for (let i = 1; i <= 5; i++) {
//     if ($('.fp-section.s'+i).hasClass('.active')) {
//     }
// }